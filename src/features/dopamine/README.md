# dopamine

Engagement widgets: popups, ticker, dashboard social proof, celebration modals.

Enable with `"dopamine"` in `features.config.ts`.

Configure fake social proof in `src/config/social-proof.config.ts` (`enabled: false` by default).

Popups are driven by `promos.config.ts` slots (`modal-training`, `toast-*`).
